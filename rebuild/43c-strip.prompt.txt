Two faults left, and they are the same three elements at two widths. Return one
fenced css block, whole, replacing your 43b block. Nothing else changes.

# What your 43b block already fixed, and must keep

    no duplicate ids              #iAim emitted once
    hovering shifts no layout     0 of 114 controls, worst 0.0px
    one background colour         html and body both rgb(242,238,230)
    every control 24px tall
    no text is cut off at 1440

19 of 21 checks pass. These two do not.

# The markup, measured, and the reason your rule did not apply

      <div class="vgroups" id="vstrip">
        <div class="vg" data-grp="ignore">
          <div class="vglab">THESE IGNORE YOUR SELECTION. EACH ANSWERS FROM ALL
                             <b>1,757,827</b> REPORTS, OR FROM A SLICE YOU SET
                             INSIDE THE PANEL</div>
          <div class="vgbtns"><button class="vtab">Fleet</button> ... </div>
        </div>
        ... three .vg in all
      </div>

You wrote this, and it is correct CSS:

    #vstrip .vglab { flex:1 1 auto; width:auto; min-width:0; max-width:360px;
                     white-space:normal; line-height:1.2 }

It does not apply. Another block on the page declares:

    #vstrip.vgroups .vglab { flex:0 0 auto; font:600 9.5px/1.2 Archivo; ... }

One id and two classes beats one id and one class, so that rule wins on
specificity, whatever the order. The computed value on the live page is still
`flex: 0 0 auto; white-space: nowrap; width: 194px`.

Write your selectors to match or beat `#vstrip.vgroups .vglab`. Do not reach for
`!important` if a selector will do it: `!important` wins arguments it should not
be in, and the next block to touch this will have the same problem you did.

# 1. The label runs under the buttons at 1440px

    <b>1,757,827</b> in .vglab   at (481, 1255), 50 x 10
    <button class="vtab">Fleet</button>  at (520, 1238), 45 x 24
    overlapping by 10 x 7

`.vglab` is `nowrap` at a fixed 194px, so a sentence runs past its own box and
under the buttons in the same flex row. The label of the second group is a whole
sentence, not a heading. Let it wrap inside its width, or give the row a shape
that fits it.

The strip must stay at or under 130px in total, which the gate checks, and all
three labels and sixteen buttons stay.

# 2. The same row runs off the screen at 390px

    document.scrollWidth 698, clientWidth 390

    the elements over the edge, none of them inside anything that clips:
      div.vg      width 502, right 533
      div.vgbtns  width 298, right 533
      button.vtab width 112, right 533

`.vg` is `display:flex; flex-direction:row; flex-wrap:nowrap`, so the label and
the button row sit side by side and their combined width exceeds the viewport.
Nothing in your block referred to `.vgbtns`, to 390px or to a media query, so
nothing addressed the button row. The label wrapping alone does not close it.

Below the phone breakpoint the three groups need a shape that fits 390: the label
above its buttons rather than beside them, and the buttons wrapping or scrolling
inside their own box. Keep all sixteen buttons and all three labels reachable,
and do not let the body scroll sideways.

At 768, 1024 and 1440 there is no horizontal overflow at all, which is better
than the parent, which breaks at 768 and doubles at 390. Do not lose that.

# What must still be true

    no text is cut off at 1440
    the tab strip at or under 130px at 1440, 16 .vtab, 3 .vglab, 16 role=tab
    every control at least 24px tall
    hovering shifts nothing
    html and body one background
    no horizontal overflow at 768, 1024 or 1440
    the case sheet untouched: no rule for #case-wrap, #case-box or
      .rr-sheet-lock, and no overflow on body
    380 month bars on the desktop strip, no runtime errors

Write no em dash and no en dash in anything that reaches the page.
